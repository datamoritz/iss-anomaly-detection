apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: iss-anomaly-system

labels:
  - pairs:
      app.kubernetes.io/name: iss-anomaly-detection
      app.kubernetes.io/part-of: iss-anomaly-platform

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - api-deployment.yaml
  - api-service.yaml
  - worker-deployment.yaml
  - ingest-deployment.yaml
  - notifications-deployment.yaml
  - injections-deployment.yaml
